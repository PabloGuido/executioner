function on_message(self, message_id, message, sender)
	if message_id == hash("create_head") then
		local p = vmath.vector3(88, 50, 0)
		local a = factory.create("/head_factory#factory", p)
		
	end
end
